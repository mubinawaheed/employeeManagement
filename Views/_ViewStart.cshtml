@{
    Layout = "_Layout";

    //just an example -- does not actually works
    @**if (User.IsInRole("admin"))
    {
        Layout = "_admin";
    }
    else
    {
        Layout = "_user";

    }**@
}
